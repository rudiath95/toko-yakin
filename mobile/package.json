{
  "name": "mobile",
  "version": "1.0.0",
  "description": "Mobile APK for Toko Yakin POS",
  "scripts": {
    "sync": "bash sync-app.sh",
    "build": "bash sync-app.sh && cd android && ./gradlew assembleDebug",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1"
  }
}
